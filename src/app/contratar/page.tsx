import React from "react";
import FormularioComponent from "../../components/formulario/formulario.component";

const Page = () => {
    return (
        <section className="w-full h-full grid place-items-center pt-[100px]">
            <FormularioComponent />
        </section>
    );
};

export default Page;
